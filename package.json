{
  "name": "smart-dairy-milk-management-system",
  "version": "1.0.0",
  "private": true,
  "description": "Production-ready MERN app for dairy milk operations",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "server": "npm run dev --prefix server",
    "client": "npm run dev --prefix client",
    "build": "npm run build --prefix client"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
